# CLI Agents HQ - Environment Configuration

# --- SERVER (HQ) CONFIG ---
PORT=5500
JWT_SECRET=your-random-string-for-auth
CLI_AGENTS_SECRET_KEY=your-shared-handshake-secret

# --- LOCAL (AGENT) CONFIG ---
# Use these when running the agent.js locally
SERVER_URL=http://localhost:5500
# CLI_AGENTS_SECRET_KEY must match the one on the server
